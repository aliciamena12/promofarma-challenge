/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["media.docmorris.de"],
        formats: ["image/avif", "image/webp"],
    },
};

export default nextConfig;
